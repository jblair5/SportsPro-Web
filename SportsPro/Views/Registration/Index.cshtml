@{

	ViewBag.Title = "Registration";

}

<div>
	<h2 class="mt-3 mb-3">Get Customer</h2>

	<form asp-action="List">
		<select name="id" asp-items="@(new SelectList(ViewBag.Customers, "CustomerID", "FullName"))" class="form-select mb-1">
			<option>Select a Customer...</option>
		</select>
		<button type="submit" class="btn btn-primary">Submit</button>

	</form>


</div>